pipeline {
    agent any

    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                sh './spring-boot-api-example-master/gradlew assemble'
            }
        }
        stage('Test') {
            steps {
                sh './spring-boot-api-example-master/gradlew test'
            }
        }
    }
}
