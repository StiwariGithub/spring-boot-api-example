pipeline {
    agent any

    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                sh 'chmod +x ./spring-boot-api-example-master/gradlew assemble'
            }
        }
        stage('Test') {
            steps {
                sh 'chmod +x ./spring-boot-api-example-master/gradlew test'
            }
        }
    }
}
